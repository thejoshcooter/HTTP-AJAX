import React from 'react';

const Form = () => {
    return (
        <div>
            <textarea type='text' placeholder='add friend here' />
            <button type='submit'>Add Friend</button>
        </div>
    );
};

export default Form;